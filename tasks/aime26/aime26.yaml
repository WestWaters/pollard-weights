# AIME 2026 -- quoted by competing low-bit releases, with no task shipped in lm-eval.
# Same shape as lm-eval's aime25 (integer answer 0-999, exact match after extraction); the dataset
# keeps all 30 problems (AIME I + II) under `train`, so that is the split scored.
tag:
  - math_word_problems
task: aime26
dataset_path: MathArena/aime_2026
output_type: generate_until
training_split: train
fewshot_split: train
test_split: train
doc_to_text: "Question: {{problem}}\nAnswer:"
doc_to_target: "{{answer}}"
process_results: !function utils.process_results
metric_list:
  - metric: exact_match
    aggregation: mean
    higher_is_better: true
generation_kwargs:
  until:
    - "Question:"
    - "</s>"
    - "<|im_end|>"
    - "<|eot_id|>"
  do_sample: false
  temperature: 0.0
  max_gen_toks: 32768
repeats: 1
num_fewshot: 0
metadata:
  version: 0.0
